function soma(a, b) {//aqui estamos declarando a function
    console.log(a + b);//para mostrar na tela
    return a + b;//para retornar o valor para usar e manipular em uma outra function no programa.
}
soma(3, 5);//aqui estamos "chamando" a function